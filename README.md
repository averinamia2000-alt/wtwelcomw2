# Whitech Helper v6.1

Changes from v6:
- default end-to-end timeout increased from 10s to 20s
- `/start` examples changed to:
  - Где найти контакты Retention?
  - Как писать недельный отчёт?
  - Могу ли я запросить себе технику?
- all v6 security scope, retrieval, context, fallback and analytics behavior retained

Railway:
Set `REQUEST_TIMEOUT_SECONDS=20` if that variable already exists in Railway.
If it does not exist, v6.1 defaults to 20 automatically.
